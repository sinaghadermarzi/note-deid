# results/ (gitignored except small summary tables)

`results/<exp>/<run-id>/{predictions.jsonl, metrics.json, cost.json, config.yaml}`; summary CSVs for the paper are
copied to `results/summary/` and committed explicitly.
